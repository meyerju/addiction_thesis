import React from 'react';

import CloudUploadIcon from '@material-ui/icons/CloudUpload';
import classes from './TitleBanner.css';

const drag = (props) => (
    <div className={classes.Title}>
        <CloudUploadIcon className={classes.Title} />
    sss    
    </div>
);

export default drag;